import styled from "styled-components";

const StyledProfileInfo = styled.div`
  width: 100%;
  height: 240px;
  background-color: aqua;
`;
const ProfileInfo = () => {
  return <StyledProfileInfo></StyledProfileInfo>;
};

export default ProfileInfo;
