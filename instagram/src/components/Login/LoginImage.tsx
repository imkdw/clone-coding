import styled from "styled-components";

const StyleLoginImage = styled.div`
  width: 350px;
  height: 700px;
  border: 1px solid;
`;

const LoginImage = () => {
  return <StyleLoginImage>LoginImage</StyleLoginImage>;
};

export default LoginImage;
