import styled from "styled-components";

const StyledForm = styled.form`
  width: 800px;
  height: 100%;
  background-color: aliceblue;
`;

function LoginForm() {
  return <StyledForm>loginform</StyledForm>;
}

export default LoginForm;
